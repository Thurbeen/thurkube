{{- if .Values.crds.install }}
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: mcpservers.thurkube.thurbeen.eu
spec:
  group: thurkube.thurbeen.eu
  names:
    categories: []
    kind: McpServer
    plural: mcpservers
    shortNames:
    - mcp
    singular: mcpserver
  scope: Namespaced
  versions:
  - additionalPrinterColumns: []
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Auto-generated derived type for McpServerSpec via `CustomResource`
        properties:
          spec:
            description: Defines an MCP server — either a local command or a remote HTTP endpoint.
            properties:
              args:
                description: Arguments for the command.
                items:
                  type: string
                type: array
              command:
                description: Command to start a local MCP server (e.g. "npx"). Mutually exclusive with `url`.
                nullable: true
                type: string
              url:
                description: URL of a remote MCP server. Mutually exclusive with `command`.
                nullable: true
                type: string
            type: object
        required:
        - spec
        title: McpServer
        type: object
    served: true
    storage: true
    subresources: {}
{{- end }}
