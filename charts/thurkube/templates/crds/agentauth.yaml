{{- if .Values.crds.install }}
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: agentauths.thurkube.thurbeen.eu
spec:
  group: thurkube.thurbeen.eu
  names:
    categories: []
    kind: AgentAuth
    plural: agentauths
    shortNames:
    - aa
    singular: agentauth
  scope: Namespaced
  versions:
  - additionalPrinterColumns: []
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Auto-generated derived type for AgentAuthSpec via `CustomResource`
        properties:
          spec:
            description: References a Kubernetes Secret key that holds the agent's authentication token.
            properties:
              secretRef:
                description: Reference to a Secret key containing the auth token.
                properties:
                  key:
                    description: Key within the Secret.
                    type: string
                  name:
                    description: Name of the Secret in the same namespace.
                    type: string
                required:
                - key
                - name
                type: object
            required:
            - secretRef
            type: object
        required:
        - spec
        title: AgentAuth
        type: object
    served: true
    storage: true
    subresources: {}
{{- end }}
